{
  "mcpServers": {
    "ConcurrentModification": {
      "command": "${LR4J_HOME}/bin/lr4j_mcp",
      "args": ["--input", "ConcurrentModificationExceptionDemo.undo", "--key", "${LR4J_KEY:-/home/ubuntu/test_key.key}"]
    }
  }
}
