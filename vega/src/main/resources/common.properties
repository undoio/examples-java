# Vega Common Configuration
# Base settings shared across all feeds and components

# Engine
engine.worker.count=4
engine.risk.free.rate=0.05
engine.quote.stale.ms=100
engine.cleanup.interval.ms=25

# Message Bus
bus.quote.capacity=100000
bus.result.capacity=50000

# Logging
log.summary.interval.ms=10000

# Server
server.shutdown.timeout.ms=5000
